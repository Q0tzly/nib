# ロードマップ

> ステータス: 合意済み（2026-09-25）

各段階は、終えた時点で CI が通り、動くものを確かめられる状態にする。性能は段階ごとに [bench/latency.py](../bench/latency.py) で測り、[architecture.md](architecture.md) の目標と比べる。

## M1 まで

M1 のゴールは、Helix 風キーマップのプラグインだけで、nib 自身のソースコードを編集してコミットできること（[vision.md](vision.md)）。

### M0.1 データモデル

> 完了（2026-09-25）

`nib-core` に、画面もプラグインもない状態でデータモデルを作る。

- バッファ（ropey）、改行コードの判定と復元
- 位置の検査、書記素の境界
- 選択（複数範囲、主選択、正規化）
- トランザクション（`edit` の列の適用、バージョンの検査）
- 線形の undo と redo（`new-step` / `merge`）
- 正規表現の検索

確かめ方: 単体テスト。

### M0.2 画面に表示する

`nib-tui` を作り、ファイルを開いて表示する。

- マス目の描画モデルと、前回との差分の出力
- 全角文字とタブの幅
- メインループ（キー入力とリサイズ）
- 入力スタックが空のときの非常用キー（終了）

プラグインがまだないので、ここでは表示と終了しかできない。

確かめ方: `nib <file>` で表示されること。bench に nib を加え、起動時間を測る。

### M0.3 プラグインホスト

- `api/` に M1 の範囲の WIT を置き、wasm-tools で検証する
- wasmtime でのプラグインの読み込み（`plugin.toml` と `plugin.wasm`）、`init` の呼び出し
- 時間とメモリの上限、止まったプラグインの片付けと再起動
- コンパイル結果のキャッシュ
- Rust SDK（`sdk/rust`）の最小版
- `cargo xtask` で、プラグインのビルドと実行ファイルへの埋め込み

確かめ方: テスト用の小さなプラグインで、キーを受け取って文字を挿入できること。無限ループするプラグインが止められ、エディタが動き続けること。

### M0.4 helix プラグインの骨格

- ノーマルモードと挿入モード
- `h` `j` `k` `l`、`i` `a` `o`、`Esc`、`d`、`u` `U`
- コマンドラインの `:w` と `:q`
- カーソルの形とステータスラインのモード表示

確かめ方: nib で簡単な編集をして保存できること。キー入力の遅延を bench で測る。

### M1 完成

- [keymap.md](keymap.md) の「M1 で実装するキー」をすべて実装する
- 複数のバッファ（`:o`、`gn` / `gp`）
- 性能目標のうち、打鍵・スクロール・起動を満たす（構文ハイライトは M2 なので、ハイライトなしで測る）

確かめ方: nib のソースを nib で編集してコミットする。

## M1 のあと

| マイルストーン | 内容 |
|----------------|------|
| M2 構文 | tree-sitter の解析基盤、WASM 形式の文法を提供するプラグイン、構文ハイライト、構文木を使うテキストオブジェクト、装飾、ポップアップ |
| M3 LSP とファイル選択 | 外部プロセス、タイマー、権限の宣言と強制、LSP プラグイン、コアの非同期ファイル列挙、ファイル選択 |
| M4 仕上げ | システムのクリップボード、分割表示、`.` による繰り返し、名前つきレジスタ、押しかけのキーの候補表示、Go SDK |

M3 を終えた時点で、git のエディタ（`core.editor`）を hx から nib に切り替える。ハイライト、LSP、ファイル選択がそろうのが M3 で、それより前に切り替えると作業のたびに hx に戻ることになる。以降は nib を普段使いしながら、困ったところから直す。

