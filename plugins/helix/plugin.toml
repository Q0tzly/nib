name = "helix"
version = "0.1.12"
api = "0.4"
# For the + register and Space y / Space p.
capabilities = ["clipboard"]
# Bracket matching waits for the tree after an edit.
events = ["editor.syntax_updated"]
