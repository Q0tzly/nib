name = "test-events"
version = "0.0.0"
api = "0.4"
capabilities = ["process", "fs-read"]
events = [
    "buffer-opened",
    "buffer-saved",
    "buffer-changed",
    "test-events.ping",
    "test-events.loop",
    "helix.mode_changed",
]
