[package]
name = "nib-plugin"
description = "SDK for writing nib plugins in Rust"
# Versioned apart from the editor: bumped only when api/ changes.
version = "0.4.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
wit-bindgen = "0.61"
